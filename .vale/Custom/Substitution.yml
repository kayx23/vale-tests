extends: substitution
message: "Use '%s' instead of '%s'." due to differences in network models.
level: suggestion
ignorecase: true
action:
  name: replace
swap:
  transport layer: l4
  application layer: l7